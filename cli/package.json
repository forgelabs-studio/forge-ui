{
  "name": "@forgelabs-studio/ui",
  "version": "0.5.0",
  "description": "Spectrum-aware, motion-first React component library. Configure visually, own the generated files.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "forge-ui": "dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "dev": "tsup --config tsup.config.ts --watch"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "commander": "^12.0.0",
    "detect-package-manager": "^3.0.0",
    "fs-extra": "^11.2.0",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.0",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
